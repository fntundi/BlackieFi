# BlackieFi 3.0 — Kubernetes Secrets
# IMPORTANT: In production, replace placeholder values before applying.
# Prefer sealing with SealedSecrets or managing via an external secrets operator.
# All values below must be base64-encoded.
#   echo -n 'your-value' | base64
apiVersion: v1
kind: Secret
metadata:
  name: blackiefi-secrets
  namespace: blackiefi
  labels:
    app.kubernetes.io/part-of: blackiefi-platform
type: Opaque
data:
  # echo -n 'CHANGE-ME-IN-PRODUCTION-minimum-32-chars!' | base64
  jwt-secret: Q0hBTkdFLU1FLUlOLVBST0RVQ1RJT04tbWluaW11bS0zMi1jaGFycyE=
  # echo -n 'admin' | base64
  mongo-root-username: YWRtaW4=
  # echo -n 'blackiefi_admin_2024' | base64
  mongo-root-password: YmxhY2tpZWZpX2FkbWluXzIwMjQ=
